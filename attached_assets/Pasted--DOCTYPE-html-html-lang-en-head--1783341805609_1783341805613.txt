<!DOCTYPE html>
<html lang="en">
<head>
    <!-- ================================================================== 
    <!-- VERITAS — Landing Page Prototype                                    
    <!-- An Automated Optical Mark & Character Recognition Grading Ledger   
    <!--                                                                     
    <!-- Frontend-only prototype.                                            
    <!-- All interactive elements expose IDs + placeholder comments for     
    <!-- future React + Vite + Firebase + OCR integration.                  
    <!-- ================================================================== 
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Veritas — AI-powered automated grading ledger. Upload paper grading sheets, OCR extracts scores, teachers verify, and export to Excel. Built for modern educators." />
    <meta name="theme-color" content="#060914" />
    <title>Veritas — Automated OCR Grading Ledger</title>

    <!-- Google Font: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

    <!-- ================================================================== -->
    <!-- STYLES                                                             -->
    <!-- ================================================================== -->
    <style>
        /* ---------- Design Tokens ---------- */
        :root {
            --bg-base: #060914;
            --bg-elevated: #0c1224;
            --bg-glass: rgba(16, 23, 44, 0.55);
            --bg-glass-strong: rgba(20, 30, 60, 0.75);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glow: rgba(45, 212, 191, 0.35);

            --text-primary: #f1f5f9;
            --text-secondary: #94a3b8;
            --text-muted: #64748b;

            --accent: #2dd4bf;       /* teal */
            --accent-2: #22d3ee;     /* cyan */
            --accent-3: #818cf8;     /* indigo */
            --accent-warm: #f59e0b;  /* amber for OCR highlight */

            --grad-hero: linear-gradient(135deg, #2dd4bf 0%, #22d3ee 45%, #818cf8 100%);
            --grad-card: linear-gradient(145deg, rgba(45, 212, 191, 0.08), rgba(129, 140, 248, 0.05));
            --grad-cta: linear-gradient(135deg, #2dd4bf 0%, #22d3ee 100%);

            --shadow-glow: 0 0 60px rgba(45, 212, 191, 0.15);
            --shadow-card: 0 20px 60px rgba(0, 0, 0, 0.45);
            --shadow-float: 0 10px 40px rgba(0, 0, 0, 0.35);

            --radius-sm: 10px;
            --radius-md: 16px;
            --radius-lg: 24px;
            --radius-xl: 32px;

            --transition: 0.4s cubic-bezier(0.22, 1, 0.36, 1);
            --transition-slow: 0.7s cubic-bezier(0.22, 1, 0.36, 1);
        }

        /* ---------- Reset ---------- */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }
        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background: var(--bg-base);
            color: var(--text-primary);
            line-height: 1.6;
            overflow-x: hidden;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }
        a { text-decoration: none; color: inherit; }
        button { font-family: inherit; cursor: pointer; border: none; background: none; color: inherit; }
        ul { list-style: none; }
        img { max-width: 100%; display: block; }

        /* ---------- Ambient background ---------- */
        .bg-ambient {
            position: fixed;
            inset: 0;
            z-index: -2;
            background:
                radial-gradient(circle at 15% 20%, rgba(45, 212, 191, 0.10), transparent 45%),
                radial-gradient(circle at 85% 30%, rgba(129, 140, 248, 0.10), transparent 45%),
                radial-gradient(circle at 50% 90%, rgba(34, 211, 238, 0.08), transparent 50%),
                var(--bg-base);
        }
        .bg-grid {
            position: fixed;
            inset: 0;
            z-index: -2;
            background-image:
                linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
            background-size: 60px 60px;
            mask-image: radial-gradient(ellipse at center, black 30%, transparent 80%);
            -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 80%);
        }
        #particles-canvas {
            position: fixed;
            inset: 0;
            z-index: -1;
            pointer-events: none;
        }

        /* ---------- Layout containers ---------- */
        .container {
            width: 100%;
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 24px;
        }
        .section {
            padding: 120px 0;
            position: relative;
        }
        .section-sm { padding: 80px 0; }

        /* ---------- Glass surface ---------- */
        .glass {
            background: var(--bg-glass);
            backdrop-filter: blur(18px) saturate(140%);
            -webkit-backdrop-filter: blur(18px) saturate(140%);
            border: 1px solid var(--border-glass);
            border-radius: var(--radius-md);
        }

        /* ---------- Typography ---------- */
        .eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 14px;
            border-radius: 999px;
            background: rgba(45, 212, 191, 0.08);
            border: 1px solid rgba(45, 212, 191, 0.25);
            color: var(--accent);
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
        }
        .section-title {
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 800;
            letter-spacing: -0.03em;
            line-height: 1.1;
            margin: 18px 0 16px;
        }
        .section-subtitle {
            color: var(--text-secondary);
            font-size: 1.05rem;
            max-width: 640px;
            margin: 0 auto;
        }
        .text-center { text-align: center; }
        .gradient-text {
            background: var(--grad-hero);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            color: transparent;
        }

        /* ---------- Buttons ---------- */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            padding: 12px 22px;
            border-radius: 12px;
            font-weight: 600;
            font-size: 0.95rem;
            transition: transform var(--transition), box-shadow var(--transition), background var(--transition);
            white-space: nowrap;
            position: relative;
            overflow: hidden;
        }
        .btn-primary {
            background: var(--grad-cta);
            color: #06121a;
            box-shadow: 0 8px 30px rgba(45, 212, 191, 0.25);
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 14px 40px rgba(45, 212, 191, 0.4);
        }
        .btn-ghost {
            background: rgba(255, 255, 255, 0.04);
            color: var(--text-primary);
            border: 1px solid var(--border-glass);
        }
        .btn-ghost:hover {
            background: rgba(255, 255, 255, 0.08);
            border-color: rgba(45, 212, 191, 0.4);
            transform: translateY(-2px);
        }
        .btn-outline {
            background: transparent;
            color: var(--accent);
            border: 1px solid rgba(45, 212, 191, 0.5);
        }
        .btn-outline:hover {
            background: rgba(45, 212, 191, 0.08);
            transform: translateY(-2px);
        }
        .btn-sm { padding: 9px 16px; font-size: 0.85rem; }

        /* ================================================================== */
        /* NAVIGATION                                                         */
        /* ================================================================== */
        .nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            transition: all var(--transition);
        }
        .nav-inner {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 18px 24px;
            max-width: 1280px;
            margin: 0 auto;
        }
        .nav.scrolled .nav-inner {
            padding: 12px 24px;
        }
        .nav.scrolled {
            background: rgba(6, 9, 20, 0.85);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-glass);
        }
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 800;
            font-size: 1.25rem;
            letter-spacing: -0.02em;
        }
        .logo-mark {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            background: var(--grad-hero);
            display: grid;
            place-items: center;
            color: #06121a;
            font-weight: 900;
            font-size: 1.1rem;
            box-shadow: 0 6px 20px rgba(45, 212, 191, 0.4);
            position: relative;
        }
        .logo-mark::after {
            content: '';
            position: absolute;
            inset: -2px;
            border-radius: 12px;
            background: var(--grad-hero);
            z-index: -1;
            opacity: 0.4;
            filter: blur(8px);
        }
        .nav-links {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .nav-links a {
            padding: 8px 16px;
            border-radius: 8px;
            color: var(--text-secondary);
            font-weight: 500;
            font-size: 0.92rem;
            transition: all var(--transition);
        }
        .nav-links a:hover {
            color: var(--text-primary);
            background: rgba(255, 255, 255, 0.04);
        }
        .nav-actions {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .nav-toggle {
            display: none;
            font-size: 1.5rem;
            color: var(--text-primary);
        }

        /* ================================================================== */
        /* HERO                                                               */
        /* ================================================================== */
        .hero {
            padding: 180px 0 100px;
            position: relative;
            overflow: hidden;
        }
        .hero-content {
            display: grid;
            grid-template-columns: 1.05fr 0.95fr;
            gap: 60px;
            align-items: center;
        }
        .hero h1 {
            font-size: clamp(2.5rem, 5.5vw, 4.25rem);
            font-weight: 900;
            line-height: 1.05;
            letter-spacing: -0.04em;
            margin: 24px 0;
        }
        .hero-sub {
            color: var(--text-secondary);
            font-size: 1.15rem;
            max-width: 560px;
            margin-bottom: 36px;
        }
        .hero-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 14px;
            margin-bottom: 48px;
        }
        .hero-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 24px;
            max-width: 520px;
        }
        .hero-stat .num {
            font-size: 1.75rem;
            font-weight: 800;
            background: var(--grad-hero);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .hero-stat .lbl {
            color: var(--text-muted);
            font-size: 0.82rem;
            margin-top: 4px;
        }

        /* Hero preview mockup */
        .hero-preview {
            position: relative;
            border-radius: var(--radius-lg);
            padding: 22px;
            background: var(--bg-glass-strong);
            backdrop-filter: blur(24px);
            -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-glass);
            box-shadow: var(--shadow-card), var(--shadow-glow);
        }
        .hero-preview-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 18px;
        }
        .preview-dots { display: flex; gap: 7px; }
        .preview-dots span {
            width: 11px; height: 11px;
            border-radius: 50%;
            background: rgba(255,255,255,0.18);
        }
        .preview-dots span:nth-child(1) { background: #f87171; }
        .preview-dots span:nth-child(2) { background: #fbbf24; }
        .preview-dots span:nth-child(3) { background: #34d399; }
        .preview-tag {
            font-size: 0.7rem;
            color: var(--accent);
            font-weight: 600;
            letter-spacing: 0.06em;
            text-transform: uppercase;
        }
        .preview-split {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }
        .preview-paper {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border-glass);
            border-radius: 12px;
            padding: 14px;
            min-height: 220px;
            position: relative;
        }
        .preview-paper::before {
            content: 'GRADING SHEET';
            position: absolute;
            top: 10px; left: 14px;
            font-size: 0.62rem;
            color: var(--text-muted);
            letter-spacing: 0.1em;
        }
        .preview-rows {
            display: flex;
            flex-direction: column;
            gap: 9px;
            margin-top: 26px;
        }
        .preview-row {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.7rem;
        }
        .preview-row .name {
            flex: 1;
            height: 8px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 4px;
        }
        .preview-row .score {
            width: 28px;
            height: 16px;
            background: rgba(245, 158, 11, 0.18);
            border: 1px solid rgba(245, 158, 11, 0.4);
            border-radius: 4px;
            position: relative;
        }
        .preview-row .score::after {
            content: attr(data-v);
            position: absolute;
            inset: 0;
            display: grid;
            place-items: center;
            color: var(--accent-warm);
            font-size: 0.6rem;
            font-weight: 700;
        }
        .preview-table {
            background: rgba(45, 212, 191, 0.04);
            border: 1px solid rgba(45, 212, 191, 0.2);
            border-radius: 12px;
            padding: 14px;
            min-height: 220px;
            position: relative;
        }
        .preview-table::before {
            content: 'VERIFIED LEDGER';
            position: absolute;
            top: 10px; left: 14px;
            font-size: 0.62rem;
            color: var(--accent);
            letter-spacing: 0.1em;
        }
        .preview-table-rows {
            display: flex;
            flex-direction: column;
            gap: 9px;
            margin-top: 26px;
        }
        .preview-table-row {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 0.7rem;
        }
        .preview-table-row .name {
            flex: 1;
            height: 8px;
            background: rgba(255, 255, 255, 0.08);
            border-radius: 4px;
        }
        .preview-table-row .score {
            width: 28px;
            height: 16px;
            background: rgba(45, 212, 191, 0.18);
            border: 1px solid rgba(45, 212, 191, 0.4);
            border-radius: 4px;
            color: var(--accent);
            font-size: 0.6rem;
            font-weight: 700;
            display: grid;
            place-items: center;
        }
        .preview-bar {
            margin-top: 14px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 12px;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 8px;
            font-size: 0.7rem;
            color: var(--text-muted);
        }
        .preview-bar .progress {
            flex: 1;
            height: 4px;
            background: rgba(255,255,255,0.08);
            border-radius: 4px;
            margin: 0 12px;
            overflow: hidden;
            position: relative;
        }
        .preview-bar .progress::after {
            content: '';
            position: absolute;
            inset: 0 30% 0 0;
            background: var(--grad-cta);
            border-radius: 4px;
            animation: progressLoop 3s ease-in-out infinite;
        }
        @keyframes progressLoop {
            0%, 100% { right: 30%; }
            50% { right: 10%; }
        }
        .float-badge {
            position: absolute;
            padding: 10px 14px;
            border-radius: 12px;
            background: var(--bg-glass-strong);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            border: 1px solid var(--border-glass);
            box-shadow: var(--shadow-float);
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.8rem;
            font-weight: 600;
            animation: floatY 5s ease-in-out infinite;
        }
        .float-badge .ic {
            width: 28px; height: 28px;
            border-radius: 8px;
            display: grid; place-items: center;
            font-size: 0.85rem;
        }
        .float-badge.b1 {
            top: -18px; right: -22px;
        }
        .float-badge.b1 .ic { background: rgba(45, 212, 191, 0.15); color: var(--accent); }
        .float-badge.b2 {
            bottom: -18px; left: -28px;
            animation-delay: 1.5s;
        }
        .float-badge.b2 .ic { background: rgba(129, 140, 248, 0.15); color: var(--accent-3); }
        @keyframes floatY {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-12px); }
        }

        /* ================================================================== */
        /* MARQUEE / TRUST                                                    */
        /* ================================================================== */
        .marquee {
            border-top: 1px solid var(--border-glass);
            border-bottom: 1px solid var(--border-glass);
            padding: 26px 0;
            background: rgba(255, 255, 255, 0.015);
            overflow: hidden;
        }
        .marquee-track {
            display: flex;
            gap: 60px;
            align-items: center;
            animation: scrollX 30s linear infinite;
            white-space: nowrap;
        }
        .marquee-track span {
            color: var(--text-muted);
            font-size: 0.95rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }
        .marquee-track i { color: var(--accent); opacity: 0.6; }
        @keyframes scrollX {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        /* ================================================================== */
        /* FEATURES                                                           */
        /* ================================================================== */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 24px;
            margin-top: 56px;
        }
        .feature-card {
            padding: 32px 28px;
            border-radius: var(--radius-md);
            background: var(--bg-glass);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--border-glass);
            transition: all var(--transition);
            position: relative;
            overflow: hidden;
        }
        .feature-card::before {
            content: '';
            position: absolute;
            inset: 0;
            background: var(--grad-card);
            opacity: 0;
            transition: opacity var(--transition);
            pointer-events: none;
        }
        .feature-card:hover {
            transform: translateY(-6px);
            border-color: rgba(45, 212, 191, 0.4);
            box-shadow: var(--shadow-card), 0 0 0 1px rgba(45, 212, 191, 0.15);
        }
        .feature-card:hover::before { opacity: 1; }
        .feature-icon {
            width: 52px;
            height: 52px;
            border-radius: 14px;
            display: grid;
            place-items: center;
            font-size: 1.4rem;
            margin-bottom: 20px;
            background: rgba(45, 212, 191, 0.12);
            color: var(--accent);
            border: 1px solid rgba(45, 212, 191, 0.25);
        }
        .feature-card:nth-child(2) .feature-icon { background: rgba(129, 140, 248, 0.12); color: var(--accent-3); border-color: rgba(129, 140, 248, 0.25); }
        .feature-card:nth-child(3) .feature-icon { background: rgba(34, 211, 238, 0.12); color: var(--accent-2); border-color: rgba(34, 211, 238, 0.25); }
        .feature-card:nth-child(4) .feature-icon { background: rgba(245, 158, 11, 0.12); color: var(--accent-warm); border-color: rgba(245, 158, 11, 0.25); }
        .feature-card:nth-child(5) .feature-icon { background: rgba(45, 212, 191, 0.12); color: var(--accent); border-color: rgba(45, 212, 191, 0.25); }
        .feature-card:nth-child(6) .feature-icon { background: rgba(129, 140, 248, 0.12); color: var(--accent-3); border-color: rgba(129, 140, 248, 0.25); }
        .feature-card h3 {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 10px;
            letter-spacing: -0.02em;
        }
        .feature-card p {
            color: var(--text-secondary);
            font-size: 0.95rem;
            margin-bottom: 22px;
            min-height: 60px;
        }
        .feature-card .view-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            color: var(--accent);
            font-weight: 600;
            font-size: 0.9rem;
            padding: 8px 16px;
            border-radius: 8px;
            background: rgba(45, 212, 191, 0.06);
            border: 1px solid rgba(45, 212, 191, 0.2);
            transition: all var(--transition);
        }
        .feature-card .view-link:hover {
            background: rgba(45, 212, 191, 0.12);
            transform: translateX(4px);
        }

        /* ================================================================== */
        /* WORKFLOW                                                           */
        /* ================================================================== */
        .workflow-wrap {
            margin-top: 60px;
            position: relative;
        }
        .workflow-grid {
            display: grid;
            grid-template-columns: repeat(6, 1fr);
            gap: 16px;
            position: relative;
        }
        .workflow-step {
            padding: 24px 18px;
            border-radius: var(--radius-md);
            background: var(--bg-glass);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            border: 1px solid var(--border-glass);
            text-align: center;
            transition: all var(--transition);
            position: relative;
            cursor: pointer;
        }
        .workflow-step:hover {
            transform: translateY(-4px);
            border-color: rgba(45, 212, 191, 0.4);
            background: rgba(45, 212, 191, 0.05);
        }
        .workflow-num {
            position: absolute;
            top: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 26px; height: 26px;
            border-radius: 50%;
            background: var(--grad-cta);
            color: #06121a;
            font-size: 0.75rem;
            font-weight: 800;
            display: grid; place-items: center;
            box-shadow: 0 4px 14px rgba(45, 212, 191, 0.4);
        }
        .workflow-icon {
            font-size: 1.8rem;
            margin: 12px 0 14px;
            color: var(--accent);
        }
        .workflow-step h4 {
            font-size: 0.98rem;
            font-weight: 700;
            margin-bottom: 6px;
        }
        .workflow-step p {
            font-size: 0.78rem;
            color: var(--text-muted);
        }
        .workflow-arrow {
            position: absolute;
            top: 50%;
            right: -14px;
            transform: translateY(-50%);
            color: var(--accent);
            font-size: 0.8rem;
            opacity: 0.6;
            z-index: 2;
        }

        /* ================================================================== */
        /* TECH STACK                                                         */
        /* ================================================================== */
        .tech-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-top: 56px;
        }
        .tech-card {
            padding: 28px 22px;
            border-radius: var(--radius-md);
            background: var(--bg-glass);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            border: 1px solid var(--border-glass);
            transition: all var(--transition);
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .tech-card::after {
            content: '';
            position: absolute;
            bottom: 0; left: 0; right: 0;
            height: 2px;
            background: var(--grad-cta);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform var(--transition-slow);
        }
        .tech-card:hover {
            transform: translateY(-6px) scale(1.02);
            border-color: rgba(45, 212, 191, 0.4);
        }
        .tech-card:hover::after { transform: scaleX(1); }
        .tech-logo {
            width: 64px;
            height: 64px;
            margin: 0 auto 16px;
            border-radius: 16px;
            display: grid;
            place-items: center;
            font-size: 1.7rem;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid var(--border-glass);
        }
        .tech-card h4 {
            font-size: 1.05rem;
            font-weight: 700;
            margin-bottom: 4px;
        }
        .tech-card .role {
            font-size: 0.78rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.06em;
        }

        /* ================================================================== */
        /* BENEFITS                                                           */
        /* ================================================================== */
        .benefits-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 22px;
            margin-top: 56px;
        }
        .benefit-card {
            padding: 28px;
            border-radius: var(--radius-md);
            background: var(--bg-glass);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            border: 1px solid var(--border-glass);
            transition: all var(--transition);
            display: flex;
            gap: 18px;
            align-items: flex-start;
        }
        .benefit-card:hover {
            transform: translateY(-4px);
            border-color: rgba(45, 212, 191, 0.35);
            background: rgba(45, 212, 191, 0.04);
        }
        .benefit-icon {
            width: 46px;
            height: 46px;
            border-radius: 12px;
            display: grid;
            place-items: center;
            font-size: 1.1rem;
            background: rgba(45, 212, 191, 0.12);
            color: var(--accent);
            flex-shrink: 0;
        }
        .benefit-card:nth-child(2) .benefit-icon { background: rgba(245, 158, 11, 0.12); color: var(--accent-warm); }
        .benefit-card:nth-child(3) .benefit-icon { background: rgba(129, 140, 248, 0.12); color: var(--accent-3); }
        .benefit-card:nth-child(4) .benefit-icon { background: rgba(34, 211, 238, 0.12); color: var(--accent-2); }
        .benefit-card:nth-child(5) .benefit-icon { background: rgba(45, 212, 191, 0.12); color: var(--accent); }
        .benefit-card:nth-child(6) .benefit-icon { background: rgba(245, 158, 11, 0.12); color: var(--accent-warm); }
        .benefit-card h4 {
            font-size: 1.08rem;
            font-weight: 700;
            margin-bottom: 6px;
        }
        .benefit-card p {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        /* ================================================================== */
        /* CTA                                                                */
        /* ================================================================== */
        .cta-wrap {
            position: relative;
            border-radius: var(--radius-xl);
            padding: 70px 50px;
            text-align: center;
            background:
                radial-gradient(circle at 30% 20%, rgba(45, 212, 191, 0.18), transparent 50%),
                radial-gradient(circle at 70% 80%, rgba(129, 140, 248, 0.18), transparent 50%),
                var(--bg-glass-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            overflow: hidden;
        }
        .cta-wrap::before {
            content: '';
            position: absolute;
            inset: 0;
            background-image:
                linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
            background-size: 40px 40px;
            mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
            -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
        }
        .cta-wrap > * { position: relative; z-index: 1; }
        .cta-wrap h2 {
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 800;
            letter-spacing: -0.03em;
            margin-bottom: 16px;
        }
        .cta-wrap p {
            color: var(--text-secondary);
            max-width: 560px;
            margin: 0 auto 36px;
            font-size: 1.05rem;
        }
        .cta-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 14px;
            justify-content: center;
        }

        /* ================================================================== */
        /* FOOTER                                                             */
        /* ================================================================== */
        .footer {
            border-top: 1px solid var(--border-glass);
            padding: 80px 0 36px;
            background: rgba(6, 9, 20, 0.6);
            position: relative;
        }
        .footer-grid {
            display: grid;
            grid-template-columns: 1.4fr 1fr 1fr 1fr;
            gap: 40px;
            margin-bottom: 60px;
        }
        .footer-brand .logo { margin-bottom: 18px; }
        .footer-brand p {
            color: var(--text-secondary);
            font-size: 0.92rem;
            max-width: 320px;
            margin-bottom: 22px;
        }
        .footer-social {
            display: flex;
            gap: 10px;
        }
        .footer-social a {
            width: 38px; height: 38px;
            border-radius: 10px;
            display: grid; place-items: center;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid var(--border-glass);
            color: var(--text-secondary);
            transition: all var(--transition);
        }
        .footer-social a:hover {
            background: rgba(45, 212, 191, 0.1);
            color: var(--accent);
            border-color: rgba(45, 212, 191, 0.3);
            transform: translateY(-2px);
        }
        .footer-col h5 {
            font-size: 0.78rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 18px;
            font-weight: 600;
        }
        .footer-col ul { display: flex; flex-direction: column; gap: 10px; }
        .footer-col a {
            color: var(--text-secondary);
            font-size: 0.92rem;
            transition: color var(--transition);
        }
        .footer-col a:hover { color: var(--accent); }
        .footer-bottom {
            padding-top: 28px;
            border-top: 1px solid var(--border-glass);
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 14px;
            color: var(--text-muted);
            font-size: 0.85rem;
        }
        .footer-bottom-links {
            display: flex;
            gap: 22px;
        }

        /* ================================================================== */
        /* SCROLL REVEAL                                                      */
        /* ================================================================== */
        .reveal {
            opacity: 0;
            transform: translateY(28px);
            transition: opacity 0.8s ease, transform 0.8s cubic-bezier(0.22, 1, 0.36, 1);
        }
        .reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .reveal-delay-1 { transition-delay: 0.1s; }
        .reveal-delay-2 { transition-delay: 0.2s; }
        .reveal-delay-3 { transition-delay: 0.3s; }
        .reveal-delay-4 { transition-delay: 0.4s; }
        .reveal-delay-5 { transition-delay: 0.5s; }

        /* ================================================================== */
        /* RESPONSIVE                                                         */
        /* ================================================================== */
        @media (max-width: 1024px) {
            .hero-content { grid-template-columns: 1fr; gap: 50px; }
            .features-grid { grid-template-columns: repeat(2, 1fr); }
            .tech-grid { grid-template-columns: repeat(3, 1fr); }
            .workflow-grid { grid-template-columns: repeat(3, 1fr); gap: 30px 16px; }
            .workflow-arrow { display: none; }
            .footer-grid { grid-template-columns: 1fr 1fr; }
        }
        @media (max-width: 768px) {
            .section { padding: 80px 0; }
            .nav-links { display: none; }
            .nav-toggle { display: block; }
            .nav-actions .btn-ghost,
            .nav-actions .btn-outline { display: none; }
            .features-grid { grid-template-columns: 1fr; }
            .tech-grid { grid-template-columns: repeat(2, 1fr); }
            .benefits-grid { grid-template-columns: 1fr; }
            .workflow-grid { grid-template-columns: 1fr; }
            .hero-stats { grid-template-columns: 1fr 1fr; max-width: 100%; }
            .footer-grid { grid-template-columns: 1fr; gap: 30px; }
            .cta-wrap { padding: 50px 24px; }
            .footer-bottom { flex-direction: column; text-align: center; }
        }
        @media (max-width: 480px) {
            .tech-grid { grid-template-columns: 1fr; }
            .hero-actions .btn { width: 100%; }
        }

        /* ---------- Mobile Nav (placeholder) ---------- */
        .mobile-nav {
            display: none;
            position: fixed;
            top: 70px;
            left: 16px;
            right: 16px;
            padding: 20px;
            border-radius: 16px;
            background: var(--bg-glass-strong);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            z-index: 999;
        }
        .mobile-nav.open { display: block; }
        .mobile-nav a {
            display: block;
            padding: 12px;
            color: var(--text-secondary);
            font-weight: 500;
            border-radius: 8px;
        }
        .mobile-nav a:hover { background: rgba(255,255,255,0.04); color: var(--text-primary); }
    </style>
</head>
<body>

    <!-- ================================================================== -->
    <!-- AMBIENT BACKGROUND LAYERS                                          -->
    <!-- ================================================================== -->
    <div class="bg-ambient" aria-hidden="true"></div>
    <div class="bg-grid" aria-hidden="true"></div>
    <canvas id="particles-canvas" aria-hidden="true"></canvas>

    <!-- ================================================================== -->
    <!-- PLACEHOLDER: FUTURE REACT APP ROOT                                 -->
    <!-- The entire <body> content below can be migrated into React         -->
    <!-- components. Each major section is wrapped in semantic <section>    -->
    <!-- tags with descriptive IDs to make porting straightforward.        -->
    <!--                                                                    -->
    <!-- Future placeholders (NOT rendered — comment anchors only):         -->
    <!--   • Login Page          →  <!-- Login Page -->                     -->
    <!--   • Register Page       →  <!-- Register Page -->                  -->
    <!--   • Dashboard           →  <!-- Dashboard -->                      -->
    <!--   • Verification Page   →  <!-- Verification Page -->              -->
    <!--   • OCR Upload          →  <!-- OCR Upload -->                     -->
    <!--   • Analytics           →  <!-- Analytics -->                      -->
    <!--   • Excel Export        →  <!-- Excel Export -->                   -->
    <!--   • Settings            →  <!-- Settings -->                       -->
    <!--   • User Profile        →  <!-- User Profile -->                   -->
    <!-- ================================================================== -->


    <!-- ================================================================== -->
    <!-- 1. NAVIGATION BAR                                                  -->
    <!-- Future React Component: <NavigationBar />                          -->
    <!-- Future Authentication: attach auth handlers to login/register      -->
    <!-- ================================================================== -->
    <nav class="nav" id="main-nav">
        <div class="nav-inner">
            <!-- Logo -->
            <a href="#hero" class="logo" id="nav-logo" aria-label="Veritas home">
                <div class="logo-mark">V</div>
                <span>Veritas</span>
            </a>

            <!-- Primary nav links -->
            <ul class="nav-links" role="menubar">
                <li role="none"><a href="#hero" id="nav-home" role="menuitem">Home</a></li>
                <li role="none"><a href="front-panel/feature.html" id="nav-features" role="menuitem">Features</a></li>
                <li role="none"><a href="front-panel/workflow.html" id="nav-workflow" role="menuitem">Workflow</a></li>
                <li role="none"><a href="front-panel/tech.html" id="nav-technology" role="menuitem">Technology</a></li>
                <li role="none"><a href="front-panel/about.html" id="nav-about" role="menuitem">About</a></li>
                <li role="none"><a href="front-panel/contact.html" id="nav-contact" role="menuitem">Contact</a></li>
            </ul>

            <!-- Action buttons -->
           <a href="front-panel/login.html"
                    class="btn btn-outline btn-sm"
                    id="login-btn">

                        Login

                    </a>
            <a href="front-panel/register.html"
                    class="btn btn-primary btn-sm"
                    id="register-btn">

                        Register

                    </a>
                <!-- Future React Component: <RouteLink to="/dashboard" /> -->
                        <a href="front-panel/getstarted.html"
                    class="btn btn-outline btn-sm"
                    id="get-started-btn">

                        Get Started

                    </a>
                <!-- Mobile nav toggle (placeholder — no logic implemented) -->
                <button class="nav-toggle" id="nav-toggle" aria-label="Toggle navigation">
                    <i class="fa-solid fa-bars"></i>
                </button>
            </div>
        </div>
    </nav>

    <!-- Mobile nav drawer — placeholder only -->
    <div class="mobile-nav" id="mobile-nav">
        <a href="#hero" id="mobile-nav-home">Home</a>
        <a href="#features" id="mobile-nav-features">Features</a>
        <a href="#workflow" id="mobile-nav-workflow">Workflow</a>
        <a href="#technology" id="mobile-nav-technology">Technology</a>
        <a href="#about" id="mobile-nav-about">About</a>
        <a href="#contact" id="mobile-nav-contact">Contact</a>
    </div>


    <!-- ================================================================== -->
    <!-- 2. HERO SECTION                                                    -->
    <!-- Future React Component: <HeroSection />                            -->
    <!-- Future React Router: <Link to="/signup"> for Get Started           -->
    <!-- Future React Router: <Link to="/demo"> for Watch Demo              -->
    <!-- ================================================================== -->
    <header class="hero" id="hero">
        <div class="container">
            <div class="hero-content">
                <!-- Hero copy -->
                <div class="hero-copy reveal">
                    <span class="eyebrow">
                        <i class="fa-solid fa-wand-magic-sparkles"></i>
                        AI-Powered Grading Ledger
                    </span>
                    <h1>
                        AI-Powered <span class="gradient-text">Automated Grading</span> System
                    </h1>
                    <p class="hero-sub">
                        Veritas turns paper grading sheets into a digital ledger in seconds. Snap a photo,
                        let OCR extract every score, verify with a split-screen editor, and export
                        clean Excel reports — all backed by a secure cloud database.
                    </p>

                    <a href="front-panel/getstarted.html"
                         class="btn btn-primary"
                            id="hero-get-started-btn">

                                <i class="fa-solid fa-rocket"></i>
                                Get Started

                                </a>

                        <!-- Future React Router: navigate to /features or scroll -->
                        <button class="btn btn-ghost" id="hero-learn-more-btn">
                            <i class="fa-solid fa-book-open"></i>
                            Learn More
                        </button>

                        <!-- Future React Component: <DemoModal /> -->
                        <button class="btn btn-outline" id="hero-watch-demo-btn">
                            <i class="fa-solid fa-play"></i>
                            Watch Demo
                        </button>
                    </div>

                    <!-- Hero stats -->
                    <div class="hero-stats">
                        <div class="hero-stat">
                            <div class="num">99.2%</div>
                            <div class="lbl">OCR Accuracy</div>
                        </div>
                        <div class="hero-stat">
                            <div class="num">10×</div>
                            <div class="lbl">Faster Encoding</div>
                        </div>
                        <div class="hero-stat">
                            <div class="num">Zero</div>
                            <div class="lbl">Manual Retyping</div>
                        </div>
                    </div>
                </div>

                <!-- Hero preview mockup -->
                <div class="hero-preview reveal reveal-delay-2">
                    <div class="hero-preview-header">
                        <div class="preview-dots"><span></span><span></span><span></span></div>
                        <div class="preview-tag">
                            <i class="fa-solid fa-circle-check"></i> Verified
                        </div>
                    </div>

                    <!-- Split-screen preview: original paper ↔ editable ledger -->
                    <div class="preview-split">
                        <div class="preview-paper">
                            <div class="preview-rows">
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="48"></span></div>
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="50"></span></div>
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="42"></span></div>
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="47"></span></div>
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="39"></span></div>
                                <div class="preview-row"><span class="name"></span><span class="score" data-v="50"></span></div>
                            </div>
                        </div>
                        <div class="preview-table">
                            <div class="preview-table-rows">
                                <div class="preview-table-row"><span class="name"></span><span class="score">48</span></div>
                                <div class="preview-table-row"><span class="name"></span><span class="score">50</span></div>
                                <div class="preview-table-row"><span class="name"></span><span class="score">42</span></div>
                                <div class="preview-table-row"><span class="name"></span><span class="score">47</span></div>
                                <div class="preview-table-row"><span class="name"></span><span class="score">39</span></div>
                                <div class="preview-table-row"><span class="name"></span><span class="score">50</span></div>
                            </div>
                        </div>
                    </div>

                    <div class="preview-bar">
                        <span><i class="fa-solid fa-bolt" style="color:var(--accent)"></i> OCR Engine</span>
                        <div class="progress"></div>
                        <span>Verifying…</span>
                    </div>

                    <!-- Floating accent badges -->
                    <div class="float-badge b1">
                        <div class="ic"><i class="fa-solid fa-scanner"></i></div>
                        <div>Scan Complete</div>
                    </div>
                    <div class="float-badge b2">
                        <div class="ic"><i class="fa-solid fa-file-export"></i></div>
                        <div>Excel Ready</div>
                    </div>
                </div>
            </div>
        </div>
    </header>


    <!-- ================================================================== -->
    <!-- TRUST MARQUEE                                                      -->
    <!-- Future React Component: <TrustBar />                               -->
    <!-- ================================================================== -->
    <div class="marquee" aria-hidden="true">
        <div class="marquee-track">
            <span><i class="fa-solid fa-shield-halved"></i> Bank-grade Encryption</span>
            <span><i class="fa-solid fa-cloud"></i> Cloud-Native</span>
            <span><i class="fa-solid fa-eye"></i> Google Vision API</span>
            <span><i class="fa-solid fa-database"></i> Firebase Realtime DB</span>
            <span><i class="fa-solid fa-file-excel"></i> ExcelJS Exports</span>
            <span><i class="fa-solid fa-bolt"></i> Real-time Verification</span>
            <span><i class="fa-solid fa-shield-halved"></i> Bank-grade Encryption</span>
            <span><i class="fa-solid fa-cloud"></i> Cloud-Native</span>
            <span><i class="fa-solid fa-eye"></i> Google Vision API</span>
            <span><i class="fa-solid fa-database"></i> Firebase Realtime DB</span>
            <span><i class="fa-solid fa-file-excel"></i> ExcelJS Exports</span>
            <span><i class="fa-solid fa-bolt"></i> Real-time Verification</span>
        </div>
    </div>


    <!-- ================================================================== -->
    <!-- 3. FEATURES SECTION                                                -->
    <!-- Future React Component: <FeaturesSection />                        -->
    <!-- Future React Router: each "View Feature" navigates to /features/:id -->
    <!-- ================================================================== -->
    <section class="section" id="features">
        <div class="container">
            <div class="text-center reveal">
                <span class="eyebrow">
                    <i class="fa-solid fa-cubes"></i> Core Features
                </span>
                <h2 class="section-title">Everything teachers need to <span class="gradient-text">grade at scale</span></h2>
                <p class="section-subtitle">
                    A complete workflow from paper to verified ledger — built to reduce administrative
                    overhead and keep academic records accurate, secure, and searchable.
                </p>
            </div>

            <div class="features-grid">
                <!-- OCR Extraction -->
                <article class="feature-card reveal" id="feature-ocr-extraction">
                    <div class="feature-icon"><i class="fa-solid fa-scanner"></i></div>
                    <h3>OCR Extraction</h3>
                    <p>Automatically detect student names, numbers, and scores from grading sheet images using Google Vision API.</p>
                    <!-- Future React Router: navigate to /features/ocr -->
                    <button class="view-link" id="view-ocr-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>

                <!-- Verification Dashboard -->
                <article class="feature-card reveal reveal-delay-1" id="feature-verification">
                    <div class="feature-icon"><i class="fa-solid fa-table-columns"></i></div>
                    <h3>Verification Dashboard</h3>
                    <p>Split-screen interface that places the original sheet beside an editable spreadsheet for instant corrections.</p>
                    <!-- Future React Router: navigate to /features/verification -->
                    <button class="view-link" id="view-verification-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>

                <!-- Analytics -->
                <article class="feature-card reveal reveal-delay-2" id="feature-analytics">
                    <div class="feature-icon"><i class="fa-solid fa-chart-line"></i></div>
                    <h3>Analytics</h3>
                    <p>Class averages, score distributions, and assessment insights — calculated the moment grades are saved.</p>
                    <!-- Future React Router: navigate to /features/analytics -->
                    <button class="view-link" id="view-analytics-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>

                <!-- Excel Export -->
                <article class="feature-card reveal" id="feature-excel-export">
                    <div class="feature-icon"><i class="fa-solid fa-file-excel"></i></div>
                    <h3>Excel Export</h3>
                    <p>Generate formatted Excel reports with averages, summary statistics, and class breakdowns via ExcelJS.</p>
                    <!-- Future React Router: navigate to /features/export -->
                    <button class="view-link" id="view-excel-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>

                <!-- Secure Database -->
                <article class="feature-card reveal reveal-delay-1" id="feature-database">
                    <div class="feature-icon"><i class="fa-solid fa-database"></i></div>
                    <h3>Secure Database</h3>
                    <p>Firebase Realtime Database stores students, classes, assessments, and scores with security rules.</p>
                    <!-- Future React Router: navigate to /features/database -->
                    <button class="view-link" id="view-database-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>

                <!-- Teacher Profiles -->
                <article class="feature-card reveal reveal-delay-2" id="feature-profiles">
                    <div class="feature-icon"><i class="fa-solid fa-user-shield"></i></div>
                    <h3>Teacher Profiles</h3>
                    <p>Per-teacher sessions, recent scans, and quick navigation — personalized dashboards for every educator.</p>
                    <!-- Future React Router: navigate to /features/profiles -->
                    <button class="view-link" id="view-profiles-btn">
                        View Feature <i class="fa-solid fa-arrow-right"></i>
                    </button>
                </article>
            </div>
        </div>
    </section>


    <!-- ================================================================== -->
    <!-- 4. SYSTEM WORKFLOW                                                 -->
    <!-- Future React Component: <WorkflowSection />                        -->
    <!-- Future Database: each card may load stats from Firebase            -->
    <!-- ================================================================== -->
    <section class="section" id="workflow" style="background: rgba(255,255,255,0.015);">
        <div class="container">
            <div class="text-center reveal">
                <span class="eyebrow">
                    <i class="fa-solid fa-diagram-project"></i> How It Works
                </span>
                <h2 class="section-title">From paper to <span class="gradient-text">verified ledger</span> in six steps</h2>
                <p class="section-subtitle">
                    Each step is designed to be observable and correctable. Teachers stay in control
                    while Veritas handles the repetitive work.
                </p>
            </div>

            <div class="workflow-wrap">
                <div class="workflow-grid">
                    <!-- Step 1 -->
                    <div class="workflow-step reveal" id="workflow-upload">
                        <span class="workflow-num">1</span>
                        <i class="fa-solid fa-cloud-arrow-up workflow-icon"></i>
                        <h4>Upload Paper</h4>
                        <p>Scan or photograph the grading sheet.</p>
                        <i class="fa-solid fa-chevron-right workflow-arrow"></i>
                    </div>

                    <!-- Step 2 -->
                    <div class="workflow-step reveal reveal-delay-1" id="workflow-ocr">
                        <span class="workflow-num">2</span>
                        <i class="fa-solid fa-eye workflow-icon"></i>
                        <h4>OCR Processing</h4>
                        <p>Google Vision reads names and scores.</p>
                        <i class="fa-solid fa-chevron-right workflow-arrow"></i>
                    </div>

                    <!-- Step 3 -->
                    <div class="workflow-step reveal reveal-delay-2" id="workflow-verify">
                        <span class="workflow-num">3</span>
                        <i class="fa-solid fa-table-columns workflow-icon"></i>
                        <h4>Verification</h4>
                        <p>Teacher reviews and edits extracted data.</p>
                        <i class="fa-solid fa-chevron-right workflow-arrow"></i>
                    </div>

                    <!-- Step 4 -->
                    <div class="workflow-step reveal reveal-delay-3" id="workflow-db">
                        <span class="workflow-num">4</span>
                        <i class="fa-solid fa-database workflow-icon"></i>
                        <h4>Database</h4>
                        <p>Records saved to Firebase Realtime Database.</p>
                        <i class="fa-solid fa-chevron-right workflow-arrow"></i>
                    </div>

                    <!-- Step 5 -->
                    <div class="workflow-step reveal reveal-delay-4" id="workflow-analytics">
                        <span class="workflow-num">5</span>
                        <i class="fa-solid fa-chart-pie workflow-icon"></i>
                        <h4>Analytics</h4>
                        <p>Instant averages and class insights.</p>
                        <i class="fa-solid fa-chevron-right workflow-arrow"></i>
                    </div>

                    <!-- Step 6 -->
                    <div class="workflow-step reveal reveal-delay-5" id="workflow-export">
                        <span class="workflow-num">6</span>
                        <i class="fa-solid fa-file-excel workflow-icon"></i>
                        <h4>Excel Export</h4>
                        <p>Download formatted grade reports.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <!-- ================================================================== -->
    <!-- 5. TECHNOLOGY STACK                                                -->
    <!-- Future React Component: <TechStackSection />                       -->
    <!-- ================================================================== -->
    <section class="section" id="technology">
        <div class="container">
            <div class="text-center reveal">
                <span class="eyebrow">
                    <i class="fa-solid fa-microchip"></i> Built With
                </span>
                <h2 class="section-title">A <span class="gradient-text">modern stack</span> for modern classrooms</h2>
                <p class="section-subtitle">
                    Veritas is engineered on a fast, type-safe, and cloud-native foundation — chosen
                    for reliability, scalability, and developer velocity.
                </p>
            </div>

            <div class="tech-grid">
                <div class="tech-card reveal" id="tech-react">
                    <div class="tech-logo" style="color:#61dafb"><i class="fa-brands fa-react"></i></div>
                    <h4>React</h4>
                    <div class="role">UI Library</div>
                </div>

                <div class="tech-card reveal reveal-delay-1" id="tech-typescript">
                    <div class="tech-logo" style="color:#3178c6"><i class="fa-solid fa-code"></i></div>
                    <h4>TypeScript</h4>
                    <div class="role">Type Safety</div>
                </div>

                <div class="tech-card reveal reveal-delay-2" id="tech-vite">
                    <div class="tech-logo" style="color:#bd34fe"><i class="fa-solid fa-bolt-lightning"></i></div>
                    <h4>Vite</h4>
                    <div class="role">Build Tool</div>
                </div>

                <div class="tech-card reveal reveal-delay-3" id="tech-firebase">
                    <div class="tech-logo" style="color:#ffa500"><i class="fa-solid fa-database"></i></div>
                    <h4>Firebase</h4>
                    <div class="role">Backend & Auth</div>
                </div>

                <div class="tech-card reveal" id="tech-realtime">
                    <div class="tech-logo" style="color:#ff6b35"><i class="fa-solid fa-server"></i></div>
                    <h4>Realtime Database</h4>
                    <div class="role">Data Storage</div>
                </div>

                <div class="tech-card reveal reveal-delay-1" id="tech-vision">
                    <div class="tech-logo" style="color:#4285f4"><i class="fa-solid fa-eye"></i></div>
                    <h4>Google Vision API</h4>
                    <div class="role">OCR Engine</div>
                </div>

                <div class="tech-card reveal reveal-delay-2" id="tech-tailwind">
                    <div class="tech-logo" style="color:#38bdf8"><i class="fa-solid fa-wind"></i></div>
                    <h4>Tailwind CSS</h4>
                    <div class="role">Styling</div>
                </div>

                <div class="tech-card reveal reveal-delay-3" id="tech-exceljs">
                    <div class="tech-logo" style="color:#217346"><i class="fa-solid fa-file-excel"></i></div>
                    <h4>ExcelJS</h4>
                    <div class="role">Report Generator</div>
                </div>
            </div>
        </div>
    </section>


    <!-- ================================================================== -->
    <!-- 6. BENEFITS SECTION                                                -->
    <!-- Future React Component: <BenefitsSection />                        -->
    <!-- ================================================================== -->
    <section class="section" id="about" style="background: rgba(255,255,255,0.015);">
        <div class="container">
            <div class="text-center reveal">
                <span class="eyebrow">
                    <i class="fa-solid fa-heart-pulse"></i> Why Veritas
                </span>
                <h2 class="section-title">Built to <span class="gradient-text">give teachers their time back</span></h2>
                <p class="section-subtitle">
                    Veritas isn't here to replace educators — it removes the repetitive, error-prone
                    work of manual encoding so teachers can focus on teaching.
                </p>
            </div>

            <div class="benefits-grid">
                <div class="benefit-card reveal" id="benefit-time">
                    <div class="benefit-icon"><i class="fa-solid fa-clock"></i></div>
                    <div>
                        <h4>Saves Time</h4>
                        <p>Turn hours of manual encoding into a few seconds of OCR-powered processing.</p>
                    </div>
                </div>

                <div class="benefit-card reveal reveal-delay-1" id="benefit-errors">
                    <div class="benefit-icon"><i class="fa-solid fa-circle-check"></i></div>
                    <div>
                        <h4>Reduces Errors</h4>
                        <p>Built-in verification catches OCR mistakes before anything reaches the database.</p>
                    </div>
                </div>

                <div class="benefit-card reveal reveal-delay-2" id="benefit-secure">
                    <div class="benefit-icon"><i class="fa-solid fa-lock"></i></div>
                    <div>
                        <h4>Secure Records</h4>
                        <p>Firebase security rules keep student data protected, encrypted, and audit-ready.</p>
                    </div>
                </div>

                <div class="benefit-card reveal" id="benefit-cloud">
                    <div class="benefit-icon"><i class="fa-solid fa-cloud"></i></div>
                    <div>
                        <h4>Cloud Storage</h4>
                        <p>Access grading ledgers from any device, anywhere — no spreadsheets to lose.</p>
                    </div>
                </div>

                <div class="benefit-card reveal reveal-delay-1" id="benefit-fast-ocr">
                    <div class="benefit-icon"><i class="fa-solid fa-bolt"></i></div>
                    <div>
                        <h4>Fast OCR</h4>
                        <p>Powered by Google Vision API through Firebase Cloud Functions for low latency.</p>
                    </div>
                </div>

                <div class="benefit-card reveal reveal-delay-2" id="benefit-reports">
                    <div class="benefit-icon"><i class="fa-solid fa-file-arrow-down"></i></div>
                    <div>
                        <h4>Easy Report Generation</h4>
                        <p>Export polished Excel reports with averages, distributions, and summaries built-in.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <!-- ================================================================== -->
    <!-- 7. CALL TO ACTION                                                  -->
    <!-- Future React Component: <CtaSection />                             -->
    <!-- Future Authentication: login & register buttons invoke Firebase    -->
    <!-- Future React Router: dashboard button routes to /dashboard         -->
    <!-- ================================================================== -->
    <section class="section-sm" id="contact">
        <div class="container">
            <div class="cta-wrap reveal">
                <span class="eyebrow">
                    <i class="fa-solid fa-rocket"></i> Get Started Today
                </span>
                <h2>Ready to <span class="gradient-text">digitize your grading</span>?</h2>
                <p>
                    Join educators using Veritas to eliminate manual encoding. Upload your first
                    grading sheet in minutes — no setup, no spreadsheets, no retyping.
                </p>

                <div class="cta-actions">
                    <!-- Future Authentication: Firebase signIn -->
                    <button class="btn btn-ghost" id="cta-login-btn">
                        <i class="fa-solid fa-right-to-bracket"></i>
                        Login
                    </button>

                    <!-- Future Authentication: Firebase signUp -->
                    <button class="btn btn-primary" id="cta-create-account-btn">
                        <i class="fa-solid fa-user-plus"></i>
                        Create Account
                    </button>

                    <!-- Future React Router: navigate to /dashboard -->
                    <button class="btn btn-outline" id="cta-dashboard-btn">
                        <i class="fa-solid fa-gauge-high"></i>
                        View Dashboard
                    </button>
                </div>
            </div>
        </div>
    </section>


    <!-- ================================================================== -->
    <!-- 8. FOOTER                                                          -->
    <!-- Future React Component: <Footer />                                 -->
    <!-- Future React Router: each link maps to a route                      -->
    <!-- ================================================================== -->
    <footer class="footer">
        <div class="container">
            <div class="footer-grid">
                <!-- Brand -->
                <div class="footer-brand">
                    <a href="#hero" class="logo">
                        <div class="logo-mark">V</div>
                        <span>Veritas</span>
                    </a>
                    <p>
                        An automated OCR grading ledger system that transforms paper-based grading
                        into a secure, searchable digital workflow.
                    </p>
                    <div class="footer-social">
                        <a href="#" id="social-github" aria-label="GitHub"><i class="fa-brands fa-github"></i></a>
                        <a href="#" id="social-twitter" aria-label="Twitter"><i class="fa-brands fa-x-twitter"></i></a>
                        <a href="#" id="social-linkedin" aria-label="LinkedIn"><i class="fa-brands fa-linkedin-in"></i></a>
                        <a href="#" id="social-email" aria-label="Email"><i class="fa-solid fa-envelope"></i></a>
                    </div>
                </div>

                <!-- Product column -->
                <div class="footer-col">
                    <h5>Product</h5>
                    <ul>
                        <li><a href="#features" id="footer-features">Features</a></li>
                        <li><a href="#workflow" id="footer-workflow">Workflow</a></li>
                        <li><a href="#technology" id="footer-technology">Technology</a></li>
                        <li><a href="#" id="footer-pricing">Pricing</a></li>
                        <li><a href="#" id="footer-changelog">Changelog</a></li>
                    </ul>
                </div>

                <!-- Resources column -->
                <div class="footer-col">
                    <h5>Resources</h5>
                    <ul>
                        <li><a href="#" id="footer-documentation">Documentation</a></li>
                        <li><a href="#" id="footer-api-reference">API Reference</a></li>
                        <li><a href="#" id="footer-support">Support</a></li>
                        <li><a href="#" id="footer-community">Community</a></li>
                        <li><a href="#" id="footer-status">System Status</a></li>
                    </ul>
                </div>

                <!-- Legal column -->
                <div class="footer-col">
                    <h5>Legal</h5>
                    <ul>
                        <li><a href="#" id="footer-privacy">Privacy</a></li>
                        <li><a href="#" id="footer-terms">Terms</a></li>
                        <li><a href="#" id="footer-security">Security</a></li>
                        <li><a href="#" id="footer-github">GitHub</a></li>
                        <li><a href="#" id="footer-contact">Contact</a></li>
                    </ul>
                </div>
            </div>

            <div class="footer-bottom">
                <div>© 2025 Veritas. All rights reserved. Built for educators.</div>
                <div class="footer-bottom-links">
                    <a href="#" id="footer-bottom-privacy">Privacy</a>
                    <a href="#" id="footer-bottom-terms">Terms</a>
                    <a href="#" id="footer-bottom-docs">Documentation</a>
                    <a href="#" id="footer-bottom-github">GitHub</a>
                    <a href="#" id="footer-bottom-contact">Contact</a>
                </div>
            </div>
        </div>
    </footer>


    <!-- ================================================================== -->
    <!-- JAVASCRIPT                                                         -->
    <!-- All scripts below are presentation-only (UI/UX).                   -->
    <!-- NO backend, auth, OCR, or database logic is implemented.           -->
    <!-- Replace placeholders with React + Firebase integration later.      -->
    <!-- ================================================================== -->
    <script>
        
    /* ================================================================== */
    /* 1. FLOATING PARTICLES BACKGROUND                                   */
    /* Purely decorative canvas animation. No business logic.             */
    /* ================================================================== */
    (function initParticles() {
        const canvas = document.getElementById('particles-canvas');
        if (!canvas) return;
        const ctx = canvas.getContext('2d');

        let particles = [];
        let w, h;

        function resize() {
            w = canvas.width = window.innerWidth;
            h = canvas.height = window.innerHeight;
        }
        resize();
        window.addEventListener('resize', resize);

        const PARTICLE_COUNT = Math.min(80, Math.floor(window.innerWidth / 18));
        for (let i = 0; i < PARTICLE_COUNT; i++) {
            particles.push({
                x: Math.random() * w,
                y: Math.random() * h,
                vx: (Math.random() - 0.5) * 0.35,
                vy: (Math.random() - 0.5) * 0.35,
                r: Math.random() * 1.8 + 0.4,
                hue: Math.random() > 0.5 ? 170 : 220
            });
        }

        function draw() {
            ctx.clearRect(0, 0, w, h);

            // particles
            particles.forEach(p => {
                p.x += p.vx;
                p.y += p.vy;
                if (p.x < 0 || p.x > w) p.vx *= -1;
                if (p.y < 0 || p.y > h) p.vy *= -1;

                ctx.beginPath();
                ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
                ctx.fillStyle = `hsla(${p.hue}, 80%, 60%, 0.55)`;
                ctx.fill();
            });

            // connections
            for (let i = 0; i < particles.length; i++) {
                for (let j = i + 1; j < particles.length; j++) {
                    const dx = particles[i].x - particles[j].x;
                    const dy = particles[i].y - particles[j].y;
                    const dist = Math.sqrt(dx * dx + dy * dy);
                    if (dist < 130) {
                        ctx.beginPath();
                        ctx.moveTo(particles[i].x, particles[i].y);
                        ctx.lineTo(particles[j].x, particles[j].y);
                        ctx.strokeStyle = `rgba(45, 212, 191, ${0.12 * (1 - dist / 130)})`;
                        ctx.lineWidth = 0.6;
                        ctx.stroke();
                    }
                }
            }
            requestAnimationFrame(draw);
        }
        draw();
    })();


    /* ================================================================== */
    /* 2. NAVBAR SCROLL STATE                                             */
    /* ================================================================== */
    (function initNavScroll() {
        const nav = document.getElementById('main-nav');
        if (!nav) return;
        const onScroll = () => {
            if (window.scrollY > 30) nav.classList.add('scrolled');
            else nav.classList.remove('scrolled');
        };
        window.addEventListener('scroll', onScroll, { passive: true });
        onScroll();
    })();


    /* ================================================================== */
    /* 3. MOBILE NAV TOGGLE (UI only — no routing)                        */
    /* ================================================================== */
    (function initMobileNav() {
        const toggle = document.getElementById('nav-toggle');
        const drawer = document.getElementById('mobile-nav');
        if (!toggle || !drawer) return;
        toggle.addEventListener('click', () => {
            drawer.classList.toggle('open');
        });
        drawer.querySelectorAll('a').forEach(a => {
            a.addEventListener('click', () => drawer.classList.remove('open'));
        });
    })();


    /* ================================================================== */
    /* 4. SCROLL REVEAL ANIMATIONS                                        */
    /* Uses IntersectionObserver — no library required.                   */
    /* ================================================================== */
    (function initReveal() {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.12, rootMargin: '0px 0px -60px 0px' });

        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    })();


    /* ================================================================== */
    /* 5. SMOOTH SCROLL FOR ANCHOR LINKS                                  */
    /* Native CSS scroll-behavior handles most cases; this adds offset    */
    /* correction for the fixed navbar.                                   */
    /* ================================================================== */
    (function initSmoothScroll() {
        document.querySelectorAll('a[href^="#"]').forEach(link => {
            link.addEventListener('click', (e) => {
                const targetId = link.getAttribute('href');
                if (!targetId || targetId === '#') return;
                const target = document.querySelector(targetId);
                if (!target) return;
                e.preventDefault();
                const top = target.getBoundingClientRect().top + window.scrollY - 80;
                window.scrollTo({ top, behavior: 'smooth' });
            });
        });
    })();


    /* ================================================================== */
    /* 6. INTERACTIVE ELEMENT PLACEHOLDERS                                */
    /*                                                                    */
    /* Every button below is wired with a console placeholder so you can  */
    /* see which ID fires. Replace these blocks with your React handlers. */
    /*                                                                    */
    /* Future Authentication:  attach Firebase auth.signIn / auth.signUp  */
    /* Future React Router:     replace with navigate('/route')           */
    /* Future OCR Integration:  trigger Firebase Cloud Function            */
    /* Future Database:         call Firebase client queries              */
    /* ================================================================== */

    // Helper: log placeholder actions
    function placeholder(id, label) {
        // eslint-disable-next-line no-console
        console.log(`[Veritas Placeholder] ${label} → id="${id}"`);
    }

    // --- Navigation buttons ---
    // Future Authentication: invoke Firebase auth flow
    document.getElementById('login-btn')?.addEventListener('click', () => {
        placeholder('login-btn', 'Login button — Future Authentication');
        // window.location.href = '/login';  // Future React Router
    });

    document.getElementById('register-btn')?.addEventListener('click', () => {
        placeholder('register-btn', 'Register button — Future Authentication');
        // window.location.href = '/register';  // Future React Router
    });

    document.getElementById('get-started-btn')?.addEventListener('click', () => {
        placeholder('get-started-btn', 'Get Started button — Future React Router');
        // window.location.href = '/dashboard';  // Future React Router
    });

    // --- Hero buttons ---
    // Future React Router: navigate to signup
    document.getElementById('hero-get-started-btn')?.addEventListener('click', () => {
        placeholder('hero-get-started-btn', 'Hero Get Started — Future React Router');
    });

    // Future React Router: scroll or navigate to features
    document.getElementById('hero-learn-more-btn')?.addEventListener('click', () => {
        placeholder('hero-learn-more-btn', 'Hero Learn More — Future React Router');
        document.getElementById('features')?.scrollIntoView({ behavior: 'smooth' });
    });

    // Future React Component: open DemoModal
    document.getElementById('hero-watch-demo-btn')?.addEventListener('click', () => {
        placeholder('hero-watch-demo-btn', 'Hero Watch Demo — Future DemoModal Component');
    });

    // --- Feature "View Feature" buttons ---
    // Future React Router: navigate to /features/:slug
    [
        ['view-ocr-btn',         'OCR Extraction'],
        ['view-verification-btn','Verification Dashboard'],
        ['view-analytics-btn',   'Analytics'],
        ['view-excel-btn',       'Excel Export'],
        ['view-database-btn',    'Secure Database'],
        ['view-profiles-btn',    'Teacher Profiles']
    ].forEach(([id, label]) => {
        document.getElementById(id)?.addEventListener('click', () => {
            placeholder(id, `${label} — Future Feature Detail Route`);
        });
    });

    // --- Workflow step cards ---
    // Future React Component: open step detail or navigate
    [
        ['workflow-upload',    'Upload Paper'],
        ['workflow-ocr',       'OCR Processing'],
        ['workflow-verify',    'Verification'],
        ['workflow-db',        'Database'],
        ['workflow-analytics', 'Analytics'],
        ['workflow-export',    'Excel Export']
    ].forEach(([id, label]) => {
        document.getElementById(id)?.addEventListener('click', () => {
            placeholder(id, `${label} step — Future Workflow Detail`);
        });
    });

    // --- Technology cards ---
    [
        ['tech-react',     'React'],
        ['tech-typescript','TypeScript'],
        ['tech-vite',      'Vite'],
        ['tech-firebase',  'Firebase'],
        ['tech-realtime',  'Realtime Database'],
        ['tech-vision',    'Google Vision API'],
        ['tech-tailwind',  'Tailwind CSS'],
        ['tech-exceljs',   'ExcelJS']
    ].forEach(([id, label]) => {
        document.getElementById(id)?.addEventListener('click', () => {
            placeholder(id, `${label} tech card`);
        });
    });

    // --- Benefit cards ---
    [
        ['benefit-time',      'Saves Time'],
        ['benefit-errors',    'Reduces Errors'],
        ['benefit-secure',    'Secure Records'],
        ['benefit-cloud',     'Cloud Storage'],
        ['benefit-fast-ocr',  'Fast OCR'],
        ['benefit-reports',   'Easy Report Generation']
    ].forEach(([id, label]) => {
        document.getElementById(id)?.addEventListener('click', () => {
            placeholder(id, `${label} benefit card`);
        });
    });

    // --- CTA buttons ---
    // Future Authentication
    document.getElementById('cta-login-btn')?.addEventListener('click', () => {
        placeholder('cta-login-btn', 'CTA Login — Future Authentication');
    });
    document.getElementById('cta-create-account-btn')?.addEventListener('click', () => {
        placeholder('cta-create-account-btn', 'CTA Create Account — Future Authentication');
    });
    document.getElementById('cta-dashboard-btn')?.addEventListener('click', () => {
        placeholder('cta-dashboard-btn', 'CTA View Dashboard — Future React Router');
    });

    // --- Footer links ---
    // Future React Router: route to each page
    [
        'footer-features', 'footer-workflow', 'footer-technology', 'footer-pricing', 'footer-changelog',
        'footer-documentation', 'footer-api-reference', 'footer-support', 'footer-community', 'footer-status',
        'footer-privacy', 'footer-terms', 'footer-security', 'footer-github', 'footer-contact',
        'footer-bottom-privacy', 'footer-bottom-terms', 'footer-bottom-docs', 'footer-bottom-github', 'footer-bottom-contact',
        'social-github', 'social-twitter', 'social-linkedin', 'social-email'
    ].forEach(id => {
        document.getElementById(id)?.addEventListener('click', (e) => {
            e.preventDefault();
            placeholder(id, 'Footer link — Future Route');
        });
    });


    /* ================================================================== */
    /* 7. FUTURE INTEGRATION ANCHORS (commented)                          */
    /*                                                                    */
    /* The following comments mark where to wire external systems once    */
    /* the React + Vite migration begins. Search this file for these      */
    /* tags to find every integration point quickly.                      */
    /*                                                                    */
    /* ---- Future React Component ----                                   */
    /* ---- Future React Router ----                                      */
    /* ---- Future Authentication ----                                    */
    /* ---- Future Database ----                                          */
    /* ---- Future OCR Integration ----                                   */
    /* ---- Future Analytics ----                                         */
    /* ---- Future Excel Export ----                                      */
    /*                                                                    */
    /* Page-level placeholders (NOT implemented, by design):              */
    /*                                                                    */
    // Login Page
    // Register Page
    // Dashboard
    // Verification Page
    // OCR Upload
    // Analytics
    // Excel Export
    // Settings
    // User Profile
    /* ================================================================== */
    </script>
</body>
</html>

